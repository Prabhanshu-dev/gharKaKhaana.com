spring:
  application:
    name: order-service

  datasource:
    url: ${DB_ORDER_URL:jdbc:mysql://localhost:3306/gkk_order_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${DB_ORDER_USER:root}
    password: ${DB_ORDER_PASS:12345}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8084
