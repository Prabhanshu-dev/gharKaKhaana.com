<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ============================================================ -->
    <!--  GharKaKhaana.com — Parent Multi-Module Aggregator POM       -->
    <!--  All child services inherit from this POM.                   -->
    <!--  No production code lives here — purely build management.    -->
    <!-- ============================================================ -->

    <groupId>org.gharKaKhaana</groupId>
    <artifactId>gharKaKhaana-parent</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>GharKaKhaana Parent</name>
    <description>Parent aggregator for GharKaKhaana microservices platform</description>

    <!-- Spring Boot parent provides default plugin management and dependency versions -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <!-- ── Microservice Modules ── -->
    <modules>
        <module>api-gateway</module>
        <module>auth-service</module>
        <module>vendor-service</module>
        <module>menu-service</module>
        <module>order-service</module>
        <module>payment-service</module>
        <module>ai-service</module>
    </modules>

    <!-- ── Global Properties ── -->
    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Dependency version pins — change in ONE place, propagates to all services -->
        <spring-cloud.version>2023.0.1</spring-cloud.version>
        <jjwt.version>0.12.6</jjwt.version>
        <mysql-connector.version>8.0.33</mysql-connector.version>
        <lombok.version>1.18.32</lombok.version>
        <razorpay.version>1.4.4</razorpay.version>
        <openai.version>4.0.0</openai.version>
        <openai-springboot.version>4.6.1</openai-springboot.version>
    </properties>

    <!-- ── Dependency Management (BOM) ── -->
    <!-- Child modules declare dependencies WITHOUT version numbers.  -->
    <!-- Versions are resolved here centrally.                        -->
    <dependencyManagement>
        <dependencies>

            <!-- Spring Cloud BOM — must be imported before individual starters -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ── JWT (JJWT 0.12.x) ── -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!-- ── Database ── -->
            <dependency>
                <groupId>mysql</groupId>
                <artifactId>mysql-connector-java</artifactId>
                <version>${mysql-connector.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!-- ── Utilities ── -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <optional>true</optional>
            </dependency>

            <!-- ── Payment ── -->
            <dependency>
                <groupId>com.razorpay</groupId>
                <artifactId>razorpay-java</artifactId>
                <version>${razorpay.version}</version>
            </dependency>

            <!-- ── AI / OpenAI ── -->
            <dependency>
                <groupId>com.openai</groupId>
                <artifactId>openai-java</artifactId>
                <version>${openai.version}</version>
            </dependency>
            <dependency>
                <groupId>com.openai</groupId>
                <artifactId>openai-java-spring-boot-starter</artifactId>
                <version>${openai-springboot.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <!-- ── Dependencies shared by ALL child modules ── -->
    <dependencies>
        <!-- Lombok available in every module for boilerplate reduction -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- Test framework available in every module -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- ── Build Plugin Management ── -->
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <!-- Exclude Lombok from the final packaged JAR -->
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>