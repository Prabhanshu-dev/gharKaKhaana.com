spring:
  application:
    name: menu-service

  datasource:
    url: ${DB_MENU_URL:jdbc:mysql://localhost:3306/gkk_menu_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${DB_MENU_USER:root}
    password: ${DB_MENU_PASS:12345}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8083
